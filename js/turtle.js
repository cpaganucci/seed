function Turtle( context )
{
    this.context = context;
}

Turtle.prototype.forward = function( amt )
{

};

Turtle.prototype.right = function( angle )
{
    this.context.rotate( angle );
};

Turtle.prototype.left = function( angle )
{

};
